import { MatrixBgDirective } from './matrix-bg.directive';

describe('MatrixBgDirective', () => {
  it('should create an instance', () => {
    const directive = new MatrixBgDirective();
    expect(directive).toBeTruthy();
  });
});
